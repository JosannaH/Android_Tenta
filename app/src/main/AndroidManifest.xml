<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.josanna.tenta_2_2022_du">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tenta_2_2022_DU">
        <activity android:name=".UpdateHorse"></activity>
        <activity android:name=".ShowAllHorses" />
        <activity android:name=".SearchHorse" />
        <activity android:name=".DeleteHorse" />
        <activity android:name=".AddHorse" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>